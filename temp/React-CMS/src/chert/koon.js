import {className as alias} from './sex';
import kos from './sex';

export default class Koon extends kos{
	sorakh = 'kooon';
	constructor(props){
  		super(props);
  	}
	orgasmAsasi(){
		return this.orgasm();
	}
}
